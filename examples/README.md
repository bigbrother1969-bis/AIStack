# AIStack Examples

This directory contains validation datasets and example workflows.

It is intentionally independent from the Python package namespace.

Examples consume the public API exposed by `aistack.*`.
